<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:BloodPressureTracker.ViewModels"
             x:Class="BloodPressureTracker.Views.MainPage"
             x:DataType="viewmodels:MainViewModel"
             Title="{Binding Title}">

	<ContentPage.Resources>
		<ResourceDictionary>
			<Color x:Key="PrimaryColor">#2196F3</Color>
			<Color x:Key="SecondaryColor">#4CAF50</Color>
			<Color x:Key="WarningColor">#FF9800</Color>
			<Color x:Key="DangerColor">#F44336</Color>

			<Style x:Key="CardStyle" TargetType="Frame">
				<Setter Property="Padding" Value="15"/>
				<Setter Property="CornerRadius" Value="10"/>
				<Setter Property="BackgroundColor" Value="{AppThemeBinding Light=White, Dark=#1E1E1E}"/>
				<Setter Property="HasShadow" Value="True"/>
				<Setter Property="BorderColor" Value="{AppThemeBinding Light=#E0E0E0, Dark=#333333}"/>
			</Style>

			<Style x:Key="PrimaryButton" TargetType="Button">
				<Setter Property="BackgroundColor" Value="{StaticResource PrimaryColor}"/>
				<Setter Property="TextColor" Value="White"/>
				<Setter Property="CornerRadius" Value="8"/>
				<Setter Property="Padding" Value="20,10"/>
				<Setter Property="FontSize" Value="14"/>
			</Style>

			<Style x:Key="SecondaryButton" TargetType="Button">
				<Setter Property="BackgroundColor" Value="Transparent"/>
				<Setter Property="TextColor" Value="{StaticResource PrimaryColor}"/>
				<Setter Property="BorderColor" Value="{StaticResource PrimaryColor}"/>
				<Setter Property="BorderWidth" Value="1"/>
				<Setter Property="CornerRadius" Value="8"/>
				<Setter Property="Padding" Value="20,10"/>
				<Setter Property="FontSize" Value="14"/>
			</Style>
		</ResourceDictionary>
	</ContentPage.Resources>

	<ContentPage.ToolbarItems>
		<ToolbarItem IconImageSource="refresh.png" Command="{Binding RefreshCommand}"/>
	</ContentPage.ToolbarItems>

	<RefreshView IsRefreshing="{Binding IsBusy}"
                 Command="{Binding RefreshCommand}">

		<ScrollView>
			<VerticalStackLayout Spacing="20" Padding="20">

				<!-- Статус сегодня -->
				<Frame Style="{StaticResource CardStyle}">
					<VerticalStackLayout Spacing="10">
						<HorizontalStackLayout>
							<Label Text="Сегодня"
                                   FontSize="20"
                                   FontAttributes="Bold"/>
							<Label Text="{Binding SelectedDate, StringFormat='{0:dd.MM.yyyy}'}"
                                   FontSize="14"
                                   VerticalOptions="Center"
                                   Margin="10,0,0,0"/>
						</HorizontalStackLayout>

						<BoxView HeightRequest="1"
                                 Color="{AppThemeBinding Light=#E0E0E0, Dark=#333333}"/>

						<HorizontalStackLayout Spacing="20">
							<!-- Утро -->
							<VerticalStackLayout Spacing="5">
								<Label Text="Утро" FontSize="12" TextColor="Gray"/>
								<Label Text="{Binding TodayRecord.MorningPressureDisplay}"
                                       FontSize="18"
                                       FontAttributes="Bold"/>
							</VerticalStackLayout>

							<!-- Обед -->
							<VerticalStackLayout Spacing="5">
								<Label Text="Обед" FontSize="12" TextColor="Gray"/>
								<Label Text="{Binding TodayRecord.AfternoonPressureDisplay}"
                                       FontSize="18"
                                       FontAttributes="Bold"/>
							</VerticalStackLayout>

							<!-- Вечер -->
							<VerticalStackLayout Spacing="5">
								<Label Text="Вечер" FontSize="12" TextColor="Gray"/>
								<Label Text="{Binding TodayRecord.EveningPressureDisplay}"
                                       FontSize="18"
                                       FontAttributes="Bold"/>
							</VerticalStackLayout>
						</HorizontalStackLayout>

						<BoxView HeightRequest="1"
                                 Color="{AppThemeBinding Light=#E0E0E0, Dark=#333333}"/>

						<!-- Статус -->
						<HorizontalStackLayout Spacing="10" VerticalOptions="Center">
							<Frame BackgroundColor="{Binding StatusColor}"
                                   CornerRadius="15"
                                   Padding="10,5"
                                   HasShadow="False">
								<Label Text="{Binding TodayStatus}"
                                       TextColor="White"
                                       FontSize="12"/>
							</Frame>

							<Button Text="➕ Запись"
                                    Command="{Binding AddRecordCommand}"
                                    Style="{StaticResource SecondaryButton}"
                                    HorizontalOptions="EndAndExpand"/>
						</HorizontalStackLayout>
					</VerticalStackLayout>
				</Frame>

				<!-- Быстрые действия -->
				<Grid ColumnDefinitions="*,*" RowDefinitions="*,*" ColumnSpacing="10" RowSpacing="10">

					<Frame Grid.Column="0" Grid.Row="0" Style="{StaticResource CardStyle}">
						<VerticalStackLayout Spacing="10" HorizontalOptions="Center">
							<Image Source="history.png" HeightRequest="40" WidthRequest="40"/>
							<Label Text="История" FontSize="14" HorizontalOptions="Center"/>
							<Button Text="Просмотр"
                                    Command="{Binding ViewHistoryCommand}"
                                    Style="{StaticResource SecondaryButton}"/>
						</VerticalStackLayout>
					</Frame>

					<Frame Grid.Column="1" Grid.Row="0" Style="{StaticResource CardStyle}">
						<VerticalStackLayout Spacing="10" HorizontalOptions="Center">
							<Image Source="chart.png" HeightRequest="40" WidthRequest="40"/>
							<Label Text="Статистика" FontSize="14" HorizontalOptions="Center"/>
							<Button Text="Анализ"
                                    Command="{Binding ViewStatisticsCommand}"
                                    Style="{StaticResource SecondaryButton}"/>
						</VerticalStackLayout>
					</Frame>

					<Frame Grid.Column="0" Grid.Row="1" Style="{StaticResource CardStyle}">
						<VerticalStackLayout Spacing="10" HorizontalOptions="Center">
							<Image Source="export.png" HeightRequest="40" WidthRequest="40"/>
							<Label Text="Экспорт" FontSize="14" HorizontalOptions="Center"/>
							<Button Text="PDF"
                                    Command="{Binding ExportDataCommand}"
                                    Style="{StaticResource SecondaryButton}"/>
						</VerticalStackLayout>
					</Frame>

					<Frame Grid.Column="1" Grid.Row="1" Style="{StaticResource CardStyle}">
						<VerticalStackLayout Spacing="10" HorizontalOptions="Center">
							<Image Source="settings.png" HeightRequest="40" WidthRequest="40"/>
							<Label Text="Настройки" FontSize="14" HorizontalOptions="Center"/>
							<Button Text="Открыть"
                                    Command="{Binding ViewSettingsCommand}"
                                    Style="{StaticResource SecondaryButton}"/>
						</VerticalStackLayout>
					</Frame>

				</Grid>

				<!-- Последние записи -->
				<Frame Style="{StaticResource CardStyle}">
					<VerticalStackLayout Spacing="10">
						<Label Text="Последние записи"
                               FontSize="18"
                               FontAttributes="Bold"/>

						<BoxView HeightRequest="1"
                                 Color="{AppThemeBinding Light=#E0E0E0, Dark=#333333}"/>

						<CollectionView ItemsSource="{Binding RecentRecords}"
                                        EmptyView="Нет записей за последние 7 дней">

							<CollectionView.ItemTemplate>
								<DataTemplate>
									<Frame Style="{StaticResource CardStyle}" Margin="0,0,0,10">
										<Grid ColumnDefinitions="*,Auto"
                                              RowDefinitions="Auto,Auto,Auto">

											<!-- Дата и день недели -->
											<VerticalStackLayout Grid.Column="0" Grid.Row="0">
												<Label Text="{Binding Date, StringFormat='{0:dd.MM.yyyy}'}"
                                                       FontSize="16"
                                                       FontAttributes="Bold"/>
												<Label Text="{Binding Date, Converter={StaticResource DayOfWeekConverter}}"
                                                       FontSize="12"
                                                       TextColor="Gray"/>
											</VerticalStackLayout>

											<!-- Действия -->
											<HorizontalStackLayout Grid.Column="1" Grid.Row="0"
                                                                   Spacing="5">
												<Button Text="✏️"
                                                        FontSize="12"
                                                        BackgroundColor="Transparent"
                                                        Padding="5"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:MainViewModel}}, 
                                                                 Path=EditRecordCommand}"
                                                        CommandParameter="{Binding .}"/>
												<Button Text="🗑️"
                                                        FontSize="12"
                                                        BackgroundColor="Transparent"
                                                        Padding="5"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:MainViewModel}}, 
                                                                 Path=DeleteRecordCommand}"
                                                        CommandParameter="{Binding .}"/>
											</HorizontalStackLayout>

											<!-- Утро -->
											<Grid Grid.Column="0" Grid.Row="1"
                                                  ColumnDefinitions="Auto,*,Auto"
                                                  Margin="0,10,0,0">
												<Label Text="Утро:"
                                                       Grid.Column="0"
                                                       FontSize="12"
                                                       TextColor="Gray"/>
												<Label Text="{Binding MorningPressureDisplay}"
                                                       Grid.Column="1"
                                                       FontSize="14"
                                                       Margin="10,0,0,0"/>
												<Label Text="{Binding PulseMorning, StringFormat='Пульс: {0}'}"
                                                       Grid.Column="2"
                                                       FontSize="12"
                                                       TextColor="Gray"/>
											</Grid>

											<!-- Обед -->
											<Grid Grid.Column="0" Grid.Row="2"
                                                  ColumnDefinitions="Auto,*,Auto"
                                                  Margin="0,5,0,0">
												<Label Text="Обед:"
                                                       Grid.Column="0"
                                                       FontSize="12"
                                                       TextColor="Gray"/>
												<Label Text="{Binding AfternoonPressureDisplay}"
                                                       Grid.Column="1"
                                                       FontSize="14"
                                                       Margin="10,0,0,0"/>
												<Label Text="{Binding PulseAfternoon, StringFormat='Пульс: {0}'}"
                                                       Grid.Column="2"
                                                       FontSize="12"
                                                       TextColor="Gray"/>
											</Grid>

											<!-- Вечер -->
											<Grid Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="3"
                                                  ColumnDefinitions="Auto,*,Auto"
                                                  Margin="0,5,0,0">
												<Label Text="Вечер:"
                                                       Grid.Column="0"
                                                       FontSize="12"
                                                       TextColor="Gray"/>
												<Label Text="{Binding EveningPressureDisplay}"
                                                       Grid.Column="1"
                                                       FontSize="14"
                                                       Margin="10,0,0,0"/>
												<Label Text="{Binding PulseEvening, StringFormat='Пульс: {0}'}"
                                                       Grid.Column="2"
                                                       FontSize="12"
                                                       TextColor="Gray"/>
											</Grid>

										</Grid>
									</Frame>
								</DataTemplate>
							</CollectionView.ItemTemplate>
						</CollectionView>
					</VerticalStackLayout>
				</Frame>

			</VerticalStackLayout>
		</ScrollView>
	</RefreshView>

</ContentPage>